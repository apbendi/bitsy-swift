{ Calculate the factorial of a postive integer }
BEGIN
  factorial_of = 10

  counter = factorial_of
  accumulator = 1

  LOOP
    IFZ counter
      BREAK
    END

    accumulator = accumulator * counter
    counter = counter - 1
  END

  PRINT accumulator
END
